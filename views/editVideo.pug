extends layouts/main

block content
    .uploadEdit__container
        form.uploadEdit__form(action=routes.editVideo(video.id), method="post")
            input(type="text", name="title", placeholder="Title Edit")
            input(type="textarea", name="description", placeholder="Description Edit")
            input(type="submit", value="Edit Video")
        a(href=routes.deleteVideo(video.id))
                button deleteVideo    